name: main
on:
  pull_request:
  push:
    branches:
      - 'main'

jobs:
  build:
    name: Build-Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: make docker-build

  test:
    name: Build-Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: make test
